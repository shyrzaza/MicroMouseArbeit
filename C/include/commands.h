#ifndef COMMANDS_H
#define COMMANDS_H

// This function gets called whenever a command is fully read via UART
// Modify it to implement your desired behaviour
void processCommand(char* command);

#endif
